# No risk management used – always returns True to allow all signals.
def no_risk_check(symbol: str, signal: str) -> bool:
    """
    Placeholder risk function that permits every signal.
    """
    return True
